# requirements.txt - DiabPredict AI dependencies
# 
# This project uses the following Python packages:
# - Streamlit: interactive web app framework
# - pandas: data manipulation and analysis
# - numpy: numerical operations
# - plotly: interactive visualizations (bar charts, scatter plots, etc.)
# - joblib: model and SHAP explainer persistence
# - scikit-learn: metrics, train/test splits, preprocessing
# - xgboost: gradient boosting model
# - shap: SHAP explainability values and plots
# - statsmodels: ARIMA time-series forecasting
# - matplotlib: static charts (used in evaluation, not the Streamlit app)
# - seaborn: statistical visualizations

streamlit = "^1.28.0"
pandas = "^2.2.0"
numpy = "^1.24.0"
plotly = "^5.13.0"
joblib = "^1.3.0"
scikit-learn = "^1.3.0"
xgboost = "^2.0.0"
shap = "^0.40.0"
statsmodels = "^0.14.0"
matplotlib = "^3.8.0"
seaborn = "^0.13.0"